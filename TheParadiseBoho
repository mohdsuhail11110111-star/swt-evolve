theparadiseboho_app/
│── android/             # Android-specific files
│── ios/                 # iOS-specific files
│── lib/                 # Main Flutter code
│   │── main.dart        # Entry point of app
│   │── screens/         # All app screens
│   │   │── splash.dart
│   │   │── home.dart
│   │   │── product_list.dart
│   │   │── cart.dart
│   │   │── checkout.dart
│   │   │── profile.dart
│   │── widgets/         # Reusable UI components
│   │── models/          # Data models (Product, User, Order)
│   │── services/        # Firebase + API integration
│── assets/              # Images, icons, fonts
│── pubspec.yaml         # Dependencies (Firebase, Razorpay, etc.)import 'package:flutter/material.dart';
import 'screens/splash.dart';

void main() {
  runApp(const ParadiseBohoApp());
}

class ParadiseBohoApp extends StatelessWidget {
  const ParadiseBohoApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'TheParadiseBoho',
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
        primaryColor: Colors.black,
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.brown),
        useMaterial3: true,
        fontFamily: 'Roboto',
      ),
      home: const SplashScreen(),
    );
  }
}import 'package:flutter/material.dart';
import 'home.dart';

class SplashScreen extends StatefulWidget {
  const SplashScreen({super.key});

  @override
  State<SplashScreen> createState() => _SplashScreenState();
}

class _SplashScreenState extends State<SplashScreen> {
  @override
  void initState() {
    super.initState();
    Future.delayed(const Duration(seconds: 3), () {
      Navigator.pushReplacement(
        context,
        MaterialPageRoute(builder: (_) => const HomeScreen()),
      );
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Image.asset("assets/logo.png", height: 120),
            const SizedBox(height: 20),
            const Text(
              "TheParadiseBoho",
              style: TextStyle(
                color: Colors.white,
                fontSize: 26,
                fontWeight: FontWeight.bold,
              ),
            )
          ],
        ),
      ),
    );
  }
}dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_messaging: ^15.0.0
  razorpay_flutter: ^1.3.5
  provider: ^6.0.0
